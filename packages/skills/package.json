{
  "name": "@cesium-nexus/skills",
  "version": "0.1.0",
  "description": "Skill dispatch and context pack builder — routes queries to specialized skills and assembles skill-aware context packs",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cesium-nexus/shared": "workspace:*",
    "@cesium-nexus/storage": "workspace:*",
    "@cesium-nexus/context-pack": "workspace:*",
    "@cesium-nexus/diagnosis": "workspace:*"
  }
}
