{
  "name": "@cesium-nexus/context-pack",
  "version": "0.1.0",
  "description": "Context Pack builder — assembles structured JSON context for LLM consumption",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cesium-nexus/shared": "workspace:*",
    "@cesium-nexus/storage": "workspace:*"
  }
}
