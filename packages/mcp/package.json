{
  "name": "@cesium-nexus/mcp",
  "version": "0.1.0",
  "description": "MCP server — 5 Cesium knowledge-base tools via stdio transport",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" }
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cesium-nexus/context-pack": "workspace:*",
    "@cesium-nexus/shared": "workspace:*",
    "@cesium-nexus/storage": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^4.4.0"
  }
}
